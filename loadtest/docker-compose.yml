services:
  k6:
    image: grafana/k6:latest
    container_name: k6-runner
    volumes:
      - ./scripts:/scripts
    working_dir: /scripts
    command: run /scripts/load_test.js
    networks:
      - app-network

networks:
  app-network:
    external: true 

